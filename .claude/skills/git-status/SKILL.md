# Git Status
Run the following commands and summarize the output:
1. `git status`
2. `git log --oneline -5`
3. `git branch -a`

Report: current branch, uncommitted changes, recent commits, and whether develop is synced with main.
